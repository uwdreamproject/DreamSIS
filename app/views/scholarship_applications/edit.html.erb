<h1>Editing scholarship application</h1>

<% semantic_form_for([@participant, @scholarship_application]) do |f| %>
  	<%= f.error_messages %>

	<%- f.inputs do -%>
		<%= render :partial => "fields", :locals => { :f => f } %>
	<% end -%>

	<%- f.buttons do -%>
	 	<%= f.submit "Update" %>
		<%= link_to 'Back', participant_scholarship_applications_path, :class => 'cancel button' %>
	<% end -%>

<% end %>