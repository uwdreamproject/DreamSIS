<%
# create a temporary form that can be used to generate the fields in the partial below
ref = nil
form_txt = content_tag :div do
  semantic_form_for([@participant, @test_score]) { |f| ref = f }
end
%>

$("#scores_fields").html("<%=j render :partial => 'scores_fields', :locals => { :f => ref } %>")

$('.section_score').change(function() {
  updateTotalScore('test_score_total_score', $(this).data("score-calculation-method"))
})
