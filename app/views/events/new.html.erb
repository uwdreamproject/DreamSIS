<h1>New event</h1>

<% semantic_form_for(@event) do |f| %>

	<%= f.error_messages %>

	<%= render :partial => "fields", :locals => { :f => f } %>

	<%= f.submit "Create", :disable_with => 'Creating...' %>
	<%= separator %>
	<%= link_to 'Cancel', events_path, :class => '' %>

<% end %>
