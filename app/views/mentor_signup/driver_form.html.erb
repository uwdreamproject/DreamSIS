<h1>Driving Agreement Form for <%= Customer.name_label %></h1>

<%= semantic_form_for @mentor, :url => { :action => 'driver_form' } do |f| %>

    <%= default_form_errors(f) %>

    <%= sanitize Customer.driver_form_content, :tags => %w(p br fieldset input label h2 ol li), :attributes => %w(id name type value for class) %>

    <hr />
    <p>Have you ever been convicted of a driving related criminal offense by either a civilian or military court? This <strong>does not include</strong> parking tickets or minor speeding tickets. This <strong>does include</strong> DUI, reckless driving/endangerment, among others. If you are unsure, please list offense(s) and we will set up a time to talk to you.</p>
    <%= f.inputs do %>
      <%= f.input :has_previous_driving_convictions, :as => :radio, :label => false %>
      <p>
        This information is only used to determine whether or not you
        can drive for <%= Customer.name_label %>. This will
        likely have no impact on whether you can join us a <%= Customer.mentor_label %>.
      </p>
      <br />
      <%= f.input :driver_form_offense_response, :label => "Date/Name of offense (if yes)" %>
    <% end %>
    <hr />
    <%= f.inputs do %>
        <%= f.input :driver_form_signature, :label => "Enter your full legal name as signature" %>
        <%= f.input :driver_form_signed_at, :as => :boolean, :label => "I understand the statements above." %>
    <% end -%>
    <%= f.actions do %>
        <%= f.action :submit, :label => "Submit" %>
    <% end -%>
<% end -%>
