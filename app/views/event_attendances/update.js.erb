if ($('#person_<%= "#{@attendee.person_id}" %>')){
  $("#<%= "person_#{@attendee.person_id}"%>").replaceWith("<%= j render :partial => 'event_attendances/person', :object => @attendee.person %>");
  //page.visual_effect :highlight, "person_#{@attendee.person_id}"
  $('#person_fullname').select();
}

if ($('#event_row_#{@attendee.event_id}')) {
  $("#event_row_<%= "#{@attendee.event_id}" %>").replaceWith("<%= j render :partial => "event_attendances/event_row", :object => @attendee %>")
}
