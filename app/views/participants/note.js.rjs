page.select('#notes').each do |element|
	page.replace_html 'notes', :partial => 'notes'
end

page.select("#notes_container_#{@participant.id}").each do |element|
	page.replace_html "notes_container_#{@participant.id}", :partial => 'notes',
															:locals => { 	:participant => @participant, 
																						:start_date => params[:start_date], 
																						:end_date => params[:end_date] }
end


page.visual_effect :highlight, "note_#{@note.id}"