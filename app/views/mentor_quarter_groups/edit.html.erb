<h1>Editing Mentor Group</h1>

<% semantic_form_for @mentor_quarter_group, :html => { :method => :put } do |f| %>

	<%= f.error_messages %>

	<%= render :partial => "fields", :locals => { :f => f } %>
	
	<%= f.submit "Save", :disable_with => 'Saving...' %>
	<span class="separator">&mdash; or &mdash;</span>
	<%= link_to 'Cancel', @mentor_quarter_group %>

<% end %>

<div id="sidebar">
	<%= render :partial => 'time_schedule_details', :object => @mentor_quarter_group.course_resource if @mentor_quarter_group.course_resource %>
</div>