<div id="sidebar">
	<h3>RSVP</h3>
	<p>Use this page to RSVP for an event. If you can't attend, please come back to change your RSVP.</p>
	
	<%- if @event -%>
		<p><%= link_to "Edit this event", edit_event_path(@event), :class => "edit button" if @current_user.try(:admin?) %></p>
		<p><%= link_to "Back to event listing", event_group_rsvp_path(@event.event_group), :class => "back button" if @event.event_group %></p>
	<% end -%>
</div>