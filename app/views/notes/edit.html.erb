<h1>Edit Note</h1>

<%= semantic_form_for @note, :html => { :multipart => true } do |f| %>

	<%= default_form_errors(f) %>

	<%= render :partial => "fields", :locals => { :f => f } %>
	
	<%= f.submit "Save", :disable_with => 'Saving...' %>
	<%= separator "or" %>
	<%= link_to 'Cancel', @note %>

<% end %>
