page.if "$('mini_participant_#{params[:mentor_term_id]}_#{@mentor_participant.participant_id}')" do
  page.visual_effect :fade, "mini_participant_#{params[:mentor_term_id]}_#{@mentor_participant.participant_id}"
end
